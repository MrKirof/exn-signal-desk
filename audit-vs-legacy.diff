# no git. baseline = artifacts/rebuild/legacy (drive snapshot)
# identical to baseline: src/routes/index.tsx scripts/grok-pwa-shared.mjs scripts/grok-pwa-plugin.test.mjs src/lib/og/site.json
--- artifacts/rebuild/legacy/src/routes/__root.tsx	2026-09-24 01:12:57.260000000 +0000
+++ src/routes/__root.tsx	2026-09-24 17:10:36.621261097 +0000
@@ -3,7 +3,7 @@
 import { PreviewHostBridge } from "@/components/preview-host-bridge";
 import appCss from "../styles.css?url";
 
-const APP_NAME = "EXN Signal Lab";
+const APP_NAME = "Manual signal desk";
 
 export const Route = createRootRoute({
   head: () => ({
@@ -14,7 +14,7 @@
       { name: "theme-color", content: "#07080a" },
       {
         name: "description",
-        content: "FX/CFD research desk: BUY/SELL/WAIT, R-multiple expectancy, next-bar backtest. No accuracy guarantee.",
+        content: "Read-only candle analysis. LONG, SHORT, or WAIT. No orders are sent.",
       },
     ],
     links: [
--- artifacts/rebuild/legacy/src/lib/lab/desk-io.ts	2026-09-24 01:12:42.589000000 +0000
+++ src/lib/lab/desk-io.ts	2026-09-24 17:14:15.393270562 +0000
@@ -16,6 +16,8 @@
   type DeskStatus,
 } from "./desk-logic.ts";
 
+export type { DeskBar } from "./desk-logic.ts";
+
 let pairUntil = 0;
 let started = false;
 
--- artifacts/rebuild/legacy/src/components/lab/panels.tsx	2026-09-24 01:12:22.774000000 +0000
+++ src/components/lab/panels.tsx	2026-09-24 17:11:58.065264621 +0000
@@ -417,7 +417,7 @@
         </Field>
       </div>
       <div className="grid gap-3">
-        <Toggle label="Auto paper (off — manual first)" checked={settings.autoPaper} onCheckedChange={(v) => patch({ autoPaper: v })} />
+        <Toggle label="Orders off — this desk is read only" checked={false} onCheckedChange={() => {}} />
         <Toggle label="News / blackout filter" checked={settings.newsFilter} onCheckedChange={(v) => patch({ newsFilter: v })} />
         <Toggle label="Separate OTC models" checked={settings.otcSeparate} onCheckedChange={(v) => patch({ otcSeparate: v })} />
         <Toggle
--- artifacts/rebuild/legacy/src/lib/lab/pipeline.ts	2026-09-24 01:12:50.339000000 +0000
+++ src/lib/lab/pipeline.ts	2026-09-24 17:14:00.493269918 +0000
@@ -18,6 +18,7 @@
 import { minutesToNews, newsBlock, readWire, type WireHit } from "./news.ts";
 import { breadthEffect, type MarketMood } from "./scan.ts";
 import { chaseBlock } from "./mind.ts";
+import { clockBreach } from "./temporal.ts";
 import { kotegawaTheory } from "./kotegawa.ts";
 import { legendCouncil } from "./legends.ts";
 import { skillClash, skillScan } from "./skill-desk.ts";
@@ -221,7 +222,7 @@
   const kote = kotegawaTheory(closed, regime);
   const legends = legendCouncil(closed, session.active, web.level === "block");
   const scan = skillScan(closed);
-  const blend = blendFamilies([...votes, kote, ...legends.votes]);
+  const blend = blendFamilies([...votes, kote]);
   const moodHit = breadthEffect(asset, blend.direction, opts.mood ?? "unknown");
   const rawModel = predictRaw(opts.model, features);
   const dirP0 =
@@ -350,6 +351,11 @@
     cancelled = chase;
     direction = ACTION.WAIT;
   }
+  const clock = last ? clockBreach(last.t, now) : "WAIT — bar clock is missing";
+  if (clock && isTrade(direction)) {
+    cancelled = clock;
+    direction = ACTION.WAIT;
+  }
   const jev = readJev(closed, regime, { spread, bid: opts.bid, ask: opts.ask });
   if (isTrade(direction) && !jevAgrees(direction, jev)) {
     cancelled = jev.note;
--- artifacts/rebuild/legacy/src/lib/lab/backtest.ts	2026-09-24 01:12:39.514000000 +0000
+++ src/lib/lab/backtest.ts	2026-09-24 17:10:48.221261599 +0000
@@ -9,6 +9,10 @@
 import { applySpread, spreadFor } from "./costs.ts";
 import { atr } from "./indicators.ts";
 import { isTrade } from "./actions.ts";
+/** A decision at index i may see bars[0..i] only. The fill, if any, is the next bar. */
+export function splitDecisionAndFill<T>(bars: T[], i: number): { seen: T[]; fill: T | undefined } {
+  return { seen: bars.slice(0, i + 1), fill: bars[i + 1] };
+}
 
 export function backtest(opts: {
   candles: Candle[];
@@ -29,7 +33,8 @@
   const blocks = new Map<string, number>();
 
   for (let i = WARMUP_BARS; i < closed.length - 2; i++) {
-    const window = closed.slice(0, i + 1);
+    const { seen: window, fill: next } = splitDecisionAndFill(closed, i);
+    if (!next) continue;
     const last = window[window.length - 1]!;
     const a = atr(
       window.map((c) => c.high),
@@ -62,8 +67,6 @@
       continue;
     }
     seen.add(signal.signalKey);
-    const next = closed[i + 1];
-    if (!next) continue;
     const entry = applySpread(next.open, signal.direction, spread);
     const stop = signal.stopPrice;
     const target = signal.targetPrice;
@@ -132,7 +135,7 @@
     .slice(0, 5)
     .map(([reason, n]) => ({ reason, n }));
   const note = outcomes.length
-    ? `${outcomes.length} closed papers on ${closed.length} bars. This is one sample, not a proven edge.`
+    ? `${outcomes.length} closed papers on ${closed.length} bars. Fills use the next bar after the signal candle. Walk-forward rows are chronological slices of this one pass, not a refitted model. Not a proven edge.`
     : `0 trades on ${closed.length} bars. The gate refused every candle. Top reason: ${topBlocks[0]?.reason ?? "none"}.`;
 
   return {
--- artifacts/rebuild/legacy/package.json	2026-09-24 01:11:52.274000000 +0000
+++ package.json	2026-09-24 17:12:29.145265965 +0000
@@ -1,11 +1,8 @@
 {
   "name": "app-builder-workspace",
-  "version": "1.0.0",
-  "author": "Desk",
   "private": true,
   "sideEffects": false,
   "type": "module",
-  "main": "desktop/main.cjs",
   "overrides": {
     "nf3": "0.3.17"
   },
@@ -17,16 +14,19 @@
     "preview": "node scripts/with-app-env.mjs vite preview",
     "preview:restart": "node scripts/preview.mjs restart",
     "preview:stop": "node scripts/preview.mjs stop",
-    "desktop": "node scripts/desktop.mjs",
     "typecheck": "tsc --noEmit",
     "check:auth": "node scripts/check-auth-invariant.mjs",
-    "test": "node --test 'scripts/**/*.test.mjs' && node --experimental-strip-types --test src/lib/app-data/app-data.test.ts src/lib/app-data/readiness-schedule.test.ts src/lib/auth/gate-identity.test.ts src/lib/auth/sign-in-gate.test.ts",
+    "test": "node --test 'scripts/**/*.test.mjs' && node --experimental-strip-types --test src/lib/app-data/app-data.test.ts src/lib/app-data/readiness-schedule.test.ts src/lib/auth/gate-identity.test.ts src/lib/auth/sign-in-gate.test.ts src/lib/lab/safety.test.ts",
     "lint": "eslint .",
     "format": "prettier --write ."
   },
   "dependencies": {
     "@electric-sql/pglite": "^0.5.4",
     "@hookform/resolvers": "^5.7.0",
+    "better-auth": "~1.6.30",
+    "jose": "6.2.9",
+    "kysely": "^0.28.5",
+    "pg": "^8.16.3",
     "@radix-ui/react-accordion": "^1.2.12",
     "@radix-ui/react-alert-dialog": "^1.1.15",
     "@radix-ui/react-avatar": "^1.1.11",
@@ -54,15 +54,11 @@
     "@tanstack/react-start": "^1.168.0",
     "@tanstack/react-table": "^8.21.0",
     "@tanstack/router-plugin": "^1.168.0",
-    "better-auth": "~1.6.30",
     "class-variance-authority": "^0.7.1",
     "clsx": "^2.1.1",
     "cmdk": "^1.1.1",
     "date-fns": "^4.0.0",
-    "jose": "6.2.9",
-    "kysely": "^0.28.5",
     "lucide-react": "^0.510.0",
-    "pg": "^8.16.3",
     "react": "^19.2.0",
     "react-day-picker": "^9.14.0",
     "react-dom": "^19.2.0",
@@ -78,14 +74,12 @@
     "zustand": "^5.0.0"
   },
   "devDependencies": {
-    "@electron/packager": "^20.3.0",
     "@eslint/js": "^9.20.0",
     "@types/node": "^22.16.5",
     "@types/pg": "^8.11.10",
     "@types/react": "^19.2.0",
     "@types/react-dom": "^19.2.0",
     "@vitejs/plugin-react": "^5.2.0",
-    "electron": "^33.4.11",
     "eslint": "^9.20.0",
     "eslint-config-prettier": "^10.1.1",
     "eslint-plugin-prettier": "^5.2.6",
--- artifacts/rebuild/legacy/src/store/lab-store.ts	2026-09-24 01:13:00.358000000 +0000
+++ src/store/lab-store.ts	2026-09-24 17:11:25.441263209 +0000
@@ -20,7 +20,7 @@
 import { ASSETS, DEFAULT_SETTINGS, MAX_HOLD_BARS, MODEL_VERSION, TIMEFRAME_SEC, assetMeta, isTrade, mulberry32 } from "@/lib/lab/layers/domain";
 import { bookFromSpot, candlesFromSpot, collectorToSpot, demoCollectorFromBars, generateHistory, newCollectorToken, normalizeBook, parseCollector, stepMarket, type MarketBook, type SpotSnapshot } from "@/lib/lab/layers/market";
 import { analyze, atr, backtest, emptyModel, PLAYBOOK, projectOutlook, similarWinRate, type MarketMood, type MarketOutlook } from "@/lib/lab/layers/signal";
-import { applyOutcome, applySpread, emptyManage, emptyRisk, expectedValue, manageOpen, mindGate, riskBlock, settlePath, settleSignal, sizeStake, spreadFor, type TradeManage } from "@/lib/lab/layers/risk";
+import { applyOutcome, emptyManage, emptyRisk, expectedValue, exnessMark, exnessOpen, manageOpen, mindGate, riskBlock, settlePath, settleSignal, sizeStake, spreadFor, type TradeManage } from "@/lib/lab/layers/risk";
 import { audit, bucketStats, calibrationFactor, closeBookRow, groupPerf, loadCandles, loadModel, loadOperations, loadOutcomes, longestLose, maxDrawdown, maybeTrain, openBookRow, pinMemory, profitFactor, putCandles, putModel, putOperation, putOutcome, putPrediction, rememberPlaybook, scoreOutcomes, seedNews, usageEstimate, wipePersonal, type BookRow } from "@/lib/lab/layers/memory";
 import { postDesk } from "@/lib/lab/layers/agents";
 
@@ -1140,10 +1140,11 @@
         lastSignalKey = "";
       } else {
         const stopDist = Math.abs(ticket.signal.entryPrice - ticket.signal.stopPrice) || 1;
+        const mark = exnessMark(ticket.signal.direction, book.price, quoteBid, quoteAsk);
         const liveR =
           (ticket.signal.direction === "BUY"
-            ? book.price - ticket.signal.entryPrice
-            : ticket.signal.entryPrice - book.price) / stopDist;
+            ? mark - ticket.signal.entryPrice
+            : ticket.signal.entryPrice - mark) / stopDist;
         open = {
           ...ticket,
           peakR: Math.max(ticket.peakR, liveR),
@@ -1198,54 +1199,12 @@
       };
     }
 
-    const block = riskBlock(risk, s.settings, now);
     const barKey = next.signalKey || `${s.asset}|${s.timeframe}|${closed.at(-1)?.t ?? 0}|${next.direction}`;
     if (isTrade(next.direction) && barKey !== lastSignalKey && !open) {
       lastSignalKey = barKey;
       void putPrediction(next);
     }
 
-    if (
-      !open &&
-      s.settings.autoPaper &&
-      isTrade(next.direction) &&
-      lifecycle === "READY" &&
-      !block &&
-      !mindGate({ open: false, outcomes, now }) &&
-      sized.stake > 0 &&
-      !sized.blocked &&
-      barKey !== lastPaperBar &&
-      !s.settings.martingaleEnabled
-    ) {
-      lastPaperBar = barKey;
-      const fill = applySpread(book.price, next.direction, spread);
-      open = {
-        signal: { ...next, entryPrice: fill, lifecycle: "SETTLING" },
-        stake: sized.stake,
-        lots: sized.lots,
-        automatic: true,
-        entryBarT: closed.at(-1)?.t ?? 0,
-        openedAt: now,
-        peakR: 0,
-        troughR: 0,
-      };
-      next = open.signal;
-      lifecycle = "SETTLING";
-      if (!bookRows.some((r) => r.id === open!.signal.signalKey)) {
-        const row = openBookRow({
-          id: open.signal.signalKey,
-          action: open.signal.direction === "SELL" ? "sell" : "buy",
-          symbol: s.asset,
-          price: open.signal.entryPrice,
-          quantity: open.lots,
-          note: `Paper ${open.signal.direction} ${s.asset}`,
-          at: now,
-        });
-        bookRows = [row, ...bookRows].slice(0, 80);
-        void putOperation(row);
-      }
-    }
-
     let manage = s.manage;
     let outlook: MarketOutlook | null = null;
     if (open) {
@@ -1380,8 +1339,8 @@
     const gen = feedGen;
     set({
       asset: a,
-      toast: "Loading live quote…",
-      health: { ...get().health, connected: false, asset: a },
+      toast: "Switching pair — last candles stay until the new quote arrives",
+      health: { ...get().health, asset: a },
     });
     pushSim(set, get, "Loading live quote…");
     void loadSpotFeed(get, gen)
@@ -1449,113 +1408,11 @@
     }
     postDesk({ action: "file", name: "settings.json", value: settings });
   },
-  paper: (manual = true) => {
-    const s = get();
-    const mind = mindGate({ open: !!s.open, outcomes: s.outcomes });
-    if (mind) {
-      set({ toast: mind });
-      return;
-    }
-    const sig = s.signal;
-    if (!sig || !isTrade(sig.direction)) return;
-    const block = riskBlock(s.risk, s.settings);
-    if (block) {
-      set({ toast: block });
-      return;
-    }
-    if (s.settings.martingaleEnabled) {
-      set({ toast: "Martingale is quarantined — turn it off to paper" });
-      return;
-    }
-    postDesk({
-      action: "step",
-      step: { at: Date.now(), asset: s.asset, direction: sig.direction, price: s.price, note: sig.cancelledReason || sig.reasons[0] || "" },
-    });
-    postDesk({ action: "runner", note: `${sig.direction} ${s.asset} stored off the screen thread` });
-    const spread =
-      s.quoteBid != null && s.quoteAsk != null ? Math.max(0, s.quoteAsk - s.quoteBid) : spreadFor(s.asset, 0);
-    const priced = { ...sig, entryPrice: applySpread(s.price, sig.direction, spread) };
-    const sized = sizeFor(s, priced);
-    if (sized.stake <= 0 || sized.blocked) {
-      const why = sized.blocked ?? "No size — risk budget";
-      set({ toast: why, sizeNote: why });
-      return;
-    }
-    const open: OpenTrade = {
-      signal: { ...priced, lifecycle: "SETTLING" },
-      stake: sized.stake,
-      lots: sized.lots,
-      automatic: !manual,
-      entryBarT: s.forming?.t ?? s.candles.at(-1)?.t ?? Date.now(),
-      openedAt: Date.now(),
-      peakR: 0,
-      troughR: 0,
-    };
-    set({
-      open,
-      manage: emptyManage(),
-      lifecycle: "SETTLING",
-      signal: open.signal,
-      sizeNote: `${sized.lots} lot · stake ${sized.stake.toFixed(2)}`,
-      toast: manual ? "Paper ticket open — watch the left card" : "Auto paper ticket",
-    });
-    persistTicket(open, s.asset, s.timeframe, s.risk);
-    const row = openBookRow({
-      id: open.signal.signalKey,
-      action: open.signal.direction === "SELL" ? "sell" : "buy",
-      symbol: s.asset,
-      price: open.signal.entryPrice,
-      quantity: open.lots,
-      note: `Paper ${open.signal.direction} ${s.asset}`,
-      at: open.openedAt,
-    });
-    set({ bookRows: [row, ...s.bookRows.filter((r) => r.id !== row.id)].slice(0, 80) });
-    void putOperation(row);
+  paper: () => {
+    set({ toast: "Read only. This desk does not place an order." });
   },
   flatten: () => {
-    const s = get();
-    const ticket = s.open;
-    if (!ticket) return;
-    const px = s.price;
-    const path = settlePath({
-      direction: ticket.signal.direction,
-      entry: ticket.signal.entryPrice,
-      stop: ticket.signal.stopPrice,
-      target: ticket.signal.targetPrice,
-      bars: [{ t: Date.now(), high: px, low: px, close: px }],
-      timeout: true,
-    });
-    const out = settleSignal(ticket.signal, path, ticket.stake, {
-      automatic: false,
-      source: "manual",
-    });
-    const risk = applyOutcome(s.risk, s.settings, out.kind === "WIN", out.kind === "TIE", out.grossProfit, Date.now());
-    void putOutcome(out);
-    postDesk({
-      action: "outcome",
-      outcome: {
-        strategy: out.strategy,
-        kind: out.kind,
-        outcomeR: out.outcomeR,
-        calibratedProbability: out.calibratedProbability,
-        settledAt: out.settledAt,
-      },
-    });
-    const row = s.bookRows.find((r) => r.id === ticket.signal.signalKey);
-    const closedRow = row ? closeBookRow(row, out.exitPrice, out.grossProfit) : null;
-    if (closedRow) void putOperation(closedRow);
-    set({
-      open: null,
-      manage: null,
-      outlook: null,
-      outcomes: [...s.outcomes, out].slice(-800),
-      bookRows: closedRow ? [closedRow, ...s.bookRows.filter((r) => r.id !== closedRow.id)].slice(0, 80) : s.bookRows,
-      risk,
-      lifecycle: "SETTLED",
-      signal: { ...ticket.signal, lifecycle: "SETTLED" },
-      toast: `Closed ${out.kind} ${out.outcomeR.toFixed(2)}R`,
-    });
-    persistTicket(null, s.asset, s.timeframe, risk);
+    set({ toast: "Read only. Nothing to close.", open: null, manage: null });
   },
   skip: () =>
     set({
--- artifacts/rebuild/legacy/src/components/lab/signal-panel.tsx	2026-09-24 01:12:23.281000000 +0000
+++ src/components/lab/signal-panel.tsx	2026-09-24 17:13:44.757269237 +0000
@@ -9,6 +9,7 @@
 import { assetMeta } from "@/lib/lab/constants";
 import { bookRisk, skillScan } from "@/lib/lab/skill-desk";
 import { readJev } from "@/lib/lab/jev";
+import { dataOrigin, originLabel } from "@/lib/lab/source-label";
 
 export function SignalPanel() {
   const lang = useLab((s) => s.settings.lang);
@@ -19,12 +20,12 @@
   const timeframe = useLab((s) => s.timeframe);
   const lifecycle = useLab((s) => s.lifecycle);
   const risk = useLab((s) => s.risk);
-  const paper = useLab((s) => s.paper);
   const skip = useLab((s) => s.skip);
+  const health = useLab((s) => s.health);
+  const collectorDemo = useLab((s) => s.collectorDemo);
+  const feedStale = useLab((s) => s.feedStale);
   const open = useLab((s) => s.open);
   const shown = open ? scanner ?? signal : signal;
-  const manage = useLab((s) => s.manage);
-  const flatten = useLab((s) => s.flatten);
   const density = useLab((s) => s.settings.density);
   const sizeNote = useLab((s) => s.sizeNote);
   const quoteBid = useLab((s) => s.quoteBid);
@@ -50,7 +51,10 @@
   const dir = shown?.direction ?? "WAIT";
   const p = shown?.calibratedProbability ?? 0.5;
   const spread = quoteBid != null && quoteAsk != null ? quoteAsk - quoteBid : assetMeta(asset).typicalSpread;
-  const dirLabel = loading ? "…" : dir === "BUY" ? tt("buy") : dir === "SELL" ? tt("sell") : tt("wait");
+  const dirLabel = loading ? "…" : dir === "BUY" ? "LONG" : dir === "SELL" ? "SHORT" : "WAIT";
+  const origin = originLabel(dataOrigin(health.source, collectorDemo), feedStale || !health.connected);
+  const calibrated = shown?.calibrationStatus === "usable" || shown?.calibrationStatus === "ok";
+  const lastClosed = candles.filter((c) => c.closed).at(-1);
   const badge = open ? shown?.lifecycle ?? "SCAN" : loading ? "COLLECTING" : lifecycle === "IDLE" ? "ANALYZING" : lifecycle;
   const cf = shown?.confluence;
 
@@ -58,7 +62,7 @@
     <aside
       data-strategies={shown?.strategies?.length ?? 0}
       data-why={shown?.cancelledReason || shown?.reasons?.[0] || "none"}
-      className="panel flex flex-col gap-3 p-4 lg:sticky lg:top-3 lg:max-h-[calc(100dvh-5.5rem)] lg:overflow-y-auto"
+      className="panel flex flex-col gap-2 p-3 lg:sticky lg:top-3 lg:max-h-[calc(100dvh-6rem)] lg:overflow-y-auto"
     >
       <div className="flex items-center justify-between gap-2">
         <p className="kicker">{open ? "Live signal" : tt("kicker")}</p>
@@ -71,7 +75,7 @@
       ) : null}
       <div
         className={cn(
-          "rounded-md px-4 py-6 text-center",
+          "rounded-md px-3 py-3 text-center",
           dir === "BUY" && "bg-call/10",
           dir === "SELL" && "bg-put/10",
           dir === "WAIT" && "panel-inset",
@@ -79,7 +83,7 @@
       >
         <p
           className={cn(
-            "display text-6xl leading-none",
+            "text-2xl font-medium leading-none",
             dir === "BUY" && "text-call",
             dir === "SELL" && "text-put",
             dir === "WAIT" && "text-muted",
@@ -95,7 +99,18 @@
           </p>
         )}
         <p className="quote mt-3 text-sm text-fg">
-          {(p * 100).toFixed(1)}% · {(shown?.expectedValueR ?? 0).toFixed(2)}R
+          {calibrated ? `${(p * 100).toFixed(1)}% calibrated` : `uncalibrated · n=${shown?.sampleSize ?? 0}`}
+          {" · "}
+          {(shown?.expectedValueR ?? 0).toFixed(2)}R scenario
+        </p>
+        <p className="mt-1 text-xs text-muted">
+          {asset} {timeframe} · {origin}
+          {lastClosed ? ` · candle ${new Date(lastClosed.t).toISOString()}` : ""}
+          {lastClosed ? ` · source ${lastClosed.source}` : ""}
+        </p>
+        <p className="mt-1 text-xs text-muted">
+          Spread assumed {fmtPx(asset, spread)}. Stop {shown?.stopPrice ? fmtPx(asset, shown.stopPrice) : "—"}. Target{" "}
+          {shown?.targetPrice ? fmtPx(asset, shown.targetPrice) : "—"}. Projection is conditional, not a promise.
         </p>
         <div className="mt-4 text-left text-xs text-muted">
           <p className="kicker">Scanner</p>
@@ -113,7 +128,7 @@
           </p>
           <p className="kicker mt-3">Jev</p>
           <p className="mt-1 text-fg">
-            {jev.action} · {(jev.confidence * 100).toFixed(0)}% · {jev.note}
+            {jev.action} · {jev.note} · rule score, uncalibrated
           </p>
           <div className="mt-2 grid grid-cols-2 gap-1">
             {jev.bots.map((bot) => (
@@ -190,7 +205,7 @@
         <p className="kicker mb-2">Strategies</p>
         <div className="grid gap-1.5">
           {(shown?.strategies.length ? shown.strategies : []).map((s) => (
-            <div key={s.name} className="panel-inset px-3 py-2">
+            <div key={s.name} className="border-b border-border py-1.5 last:border-0">
               <div className="flex items-center justify-between gap-2 text-xs">
                 <span className="font-medium">{s.name.replaceAll("_", " ")}</span>
                 <span
@@ -233,21 +248,12 @@
         )}
       </div>
 
+      <p className="text-xs text-muted">Read only. No order is sent from this desk.</p>
       <div className="sticky bottom-0 mt-auto flex gap-2 bg-surface pt-2">
-        {open ? (
-          <Button className="flex-1" variant={manage?.action === "EXIT_EARLY" || manage?.action === "TIME_STOP" ? "put" : "secondary"} onClick={flatten}>
-            Close ticket
-          </Button>
-        ) : (
-          <Button className="flex-1" variant={dir === "SELL" ? "put" : "call"} disabled={dir === "WAIT" || loading} onClick={() => paper(true)}>
-            {tt("paper")}
-          </Button>
-        )}
         <Button className="flex-1" variant="secondary" onClick={skip} disabled={!!open}>
           {tt("skip")}
         </Button>
       </div>
-      {!open && dir === "WAIT" ? <p className="text-xs text-muted">Paper stays off until the badge says BUY or SELL.</p> : null}
       {sizeNote ? <p className="text-xs text-muted">{sizeNote}</p> : null}
     </aside>
   );
--- artifacts/rebuild/legacy/src/components/lab/lab-shell.tsx	2026-09-24 01:12:21.789000000 +0000
+++ src/components/lab/lab-shell.tsx	2026-09-24 17:13:45.485269268 +0000
@@ -81,9 +81,6 @@
   const signal = useLab((s) => s.signal);
   const openTrade = useLab((s) => s.open);
   const scanner = useLab((s) => s.scanner);
-  const netOnline = useLab((s) => s.netOnline);
-  const collectorLive = useLab((s) => s.collectorLive);
-  const collectorListening = useLab((s) => s.collectorListening);
   const tape = openTrade ? scanner ?? signal : signal;
 
   useEffect(() => {
@@ -160,15 +157,14 @@
     : health.source === "simulated" || feedStale
       ? "led-warn"
       : "led-live";
-  const spread = quoteBid != null && quoteAsk != null ? quoteAsk - quoteBid : null;
 
   return (
     <div className="flex min-h-dvh overflow-x-hidden bg-bg text-fg">
-      <aside className="hidden w-16 shrink-0 flex-col border-r border-border bg-surface lg:flex">
-        <div className="flex h-16 items-center justify-center border-b border-border">
-          <span className="font-mono text-xs font-medium tracking-widest text-accent">EXN</span>
+      <aside className="hidden w-20 shrink-0 flex-col border-r border-border bg-surface lg:flex">
+        <div className="flex h-14 items-center justify-center border-b border-border">
+          <span className="font-mono text-xs font-medium text-accent">EXN</span>
         </div>
-        <nav className="flex flex-1 flex-col gap-1 p-2" aria-label="Primary">
+        <nav className="flex flex-1 flex-col gap-0.5 p-1.5" aria-label="Primary">
           {VIEWS.map((v) => {
             const Icon = v.icon;
             return (
@@ -179,12 +175,12 @@
                 onClick={() => setView(v.id)}
                 title={t(lang, v.key)}
                 className={cn(
-                  "flex h-11 w-full items-center justify-center rounded-sm text-muted transition-[background-color,color] duration-150",
+                  "flex h-12 w-full flex-col items-center justify-center gap-0.5 rounded-md text-muted",
                   view === v.id ? "bg-elevated text-fg" : "hover:bg-elevated hover:text-fg",
                 )}
               >
-                <Icon className="size-4" strokeWidth={1.6} />
-                <span className="sr-only">{t(lang, v.key)}</span>
+                <Icon className="size-4" strokeWidth={1.7} />
+                <span className="max-w-full truncate text-xs leading-none">{t(lang, v.key)}</span>
               </button>
             );
           })}
@@ -193,13 +189,36 @@
       </aside>
 
       <div className="flex min-w-0 flex-1 flex-col">
-        <header className="border-b border-border bg-surface/95 backdrop-blur-sm">
-          <div className="flex items-center gap-2 overflow-x-auto px-3 py-2 lg:px-4">
-            <div className="shrink-0">
-              <p className="kicker">{APP_NAME}</p>
-              <h1 className="hidden text-sm font-medium tracking-tight sm:block">{t(lang, "kicker")}</h1>
+        <header className="border-b border-border bg-surface">
+          <div className="flex h-14 items-center gap-3 px-3 lg:px-4">
+            <div className="min-w-0 shrink-0">
+              <p className="truncate text-sm font-medium">{APP_NAME}</p>
+              <p className="hidden truncate text-xs text-muted sm:block">{feedLabel}</p>
             </div>
-
+            <div className="flex items-baseline gap-3">
+              <span className={cn("led", ledClass)} aria-hidden />
+              <p className="quote text-lg leading-none">{fmtPx(asset, price)}</p>
+            </div>
+            {quoteBid != null && quoteAsk != null && (
+              <div className="hidden font-mono text-xs tabular-nums sm:block">
+                <p className="text-call">Ask {fmtPx(asset, quoteAsk)}</p>
+                <p className="text-put">Bid {fmtPx(asset, quoteBid)}</p>
+              </div>
+            )}
+            <p className={cn("hidden font-mono text-sm sm:block", risk.dailyPnl >= 0 ? "text-call" : "text-put")}>{fmtUsd(risk.dailyPnl)}</p>
+            <div className="ml-auto flex shrink-0 items-center gap-2">
+              {locked && <Badge variant="put">Locked</Badge>}
+              <AccountBox compact />
+              <UtcClock />
+              <Button size="sm" variant="ghost" onClick={() => patch({ lang: lang === "en" ? "bn" : "en" })}>
+                {lang === "en" ? "BN" : "EN"}
+              </Button>
+              <Button size="sm" variant="ghost" className="hidden sm:inline-flex" onClick={() => patch({ theme: theme === "dark" ? "light" : "dark" })}>
+                {theme === "dark" ? "Light" : "Dark"}
+              </Button>
+            </div>
+          </div>
+          <div className="flex items-center gap-2 overflow-x-auto border-t border-border px-3 py-2 lg:px-4">
             <div className="seg shrink-0">
               {ASSETS.map((a) => (
                 <button
@@ -217,77 +236,33 @@
             </div>
             <div className="seg shrink-0">
               {TFS.map((tf) => (
-                <button key={tf} className="seg-btn" data-on={timeframe === tf ? "true" : "false"} onClick={() => setTimeframe(tf)}>
+                <button key={tf} type="button" className="seg-btn" data-on={timeframe === tf ? "true" : "false"} onClick={() => setTimeframe(tf)}>
                   {tf}
                 </button>
               ))}
             </div>
-
-            <div className="flex min-w-0 shrink-0 items-center gap-2 sm:gap-3">
-              <span className={cn("led", ledClass)} aria-hidden />
-              <div className="min-w-0">
-                <p className="quote text-2xl leading-none text-accent sm:text-4xl">{fmtPx(asset, price)}</p>
-                <p className="hidden truncate font-mono text-xs text-muted sm:block">
-                  {feedLabel}
-                  {spread != null ? ` · spr ${fmtPx(asset, spread)}` : ""}
-                </p>
-              </div>
-              {quoteBid != null && quoteAsk != null && (
-                <div className="hidden font-mono text-xs tabular-nums md:block">
-                  <p className="text-call">A {fmtPx(asset, quoteAsk)}</p>
-                  <p className="text-put">B {fmtPx(asset, quoteBid)}</p>
-                </div>
-              )}
-              <div className="hidden font-mono text-xs text-muted lg:block">
-                <p className={netOnline ? "text-call" : "text-put"}>{netOnline ? "Internet on" : "Internet off"}</p>
-                <p>{collectorLive ? "Extension live" : collectorListening ? "Extension waiting" : "Extension off"}</p>
-              </div>
-            </div>
-
-            <div className="ml-auto flex shrink-0 items-center gap-1 sm:gap-2">
-              {locked && <Badge variant="put">LOCKED</Badge>}
-              <AccountBox compact />
-              <span className={cn("quote text-sm", risk.dailyPnl >= 0 ? "text-call" : "text-put")}>{fmtUsd(risk.dailyPnl)}</span>
-              <UtcClock />
-              <Button size="sm" variant="ghost" onClick={() => patch({ lang: lang === "en" ? "bn" : "en" })}>
-                {lang === "en" ? "BN" : "EN"}
-              </Button>
-              <Button size="sm" variant="ghost" className="hidden sm:inline-flex" onClick={() => patch({ theme: theme === "dark" ? "light" : "dark" })}>
-                {theme === "dark" ? "Light" : "Dark"}
-              </Button>
-              <Button size="sm" variant="ghost" className="hidden md:inline-flex" onClick={() => patch({ density: density === "advanced" ? "compact" : "advanced" })}>
-                {t(lang, density === "advanced" ? "compact" : "advanced")}
-              </Button>
-            </div>
+            {tape && (
+              <p className="hidden min-w-0 truncate font-mono text-xs text-muted md:block">
+                {regimeLabel(tape.regime)} · {tape.session?.label ?? "—"} · {tape.structure?.zone ?? "—"} · CF {tape.confluence?.score ?? 0}/{tape.confluence?.max ?? 0}
+              </p>
+            )}
+            <nav className="ml-auto hidden gap-1 sm:flex lg:hidden" aria-label="Screens">
+              {VIEWS.map((v) => (
+                <button
+                  key={v.id}
+                  type="button"
+                  data-view-id={v.id}
+                  onClick={() => setView(v.id)}
+                  className={cn(
+                    "shrink-0 rounded-md px-2.5 py-1.5 text-sm",
+                    view === v.id ? "bg-elevated text-fg" : "text-muted hover:text-fg",
+                  )}
+                >
+                  {t(lang, v.key)}
+                </button>
+              ))}
+            </nav>
           </div>
-          <nav className="flex gap-1 overflow-x-auto border-t border-border px-3 py-1.5 lg:px-4" aria-label="Screens">
-            {VIEWS.map((v) => (
-              <button
-                key={v.id}
-                type="button"
-                data-view-id={v.id}
-                onClick={() => setView(v.id)}
-                className={cn(
-                  "shrink-0 rounded-sm px-2 py-1 text-xs",
-                  view === v.id ? "bg-elevated text-fg" : "text-muted hover:text-fg",
-                )}
-              >
-                {t(lang, v.key)}
-              </button>
-            ))}
-          </nav>
-          {tape && (
-            <div className="flex flex-wrap items-center gap-x-4 gap-y-1 border-t border-border px-3 py-1.5 font-mono text-xs text-muted lg:px-4">
-              <span>{openTrade ? "scan" : "plan"}</span>
-              <span>{regimeLabel(tape.regime)}</span>
-              <span>{tape.session?.label ?? "—"}</span>
-              <span>{tape.structure?.zone ?? "—"}</span>
-              <span>
-                CF {tape.confluence?.score ?? 0}/{tape.confluence?.max ?? 0} {tape.confluence?.grade ?? ""}
-              </span>
-              <span className="ml-auto hidden sm:inline">{t(lang, view === "desk" ? "desk" : view)}</span>
-            </div>
-          )}
         </header>
 
         {view === "desk" && <MarketScan />}
@@ -423,11 +398,11 @@
   const outlook = useLab((s) => s.outlook);
   const pane = typeof window === "undefined" ? null : new URLSearchParams(window.location.search).get("pane");
   return (
-    <div className="grid gap-4 xl:grid-cols-[minmax(0,1fr)_380px]">
+    <div className="grid items-start gap-3 xl:grid-cols-[minmax(0,1fr)_300px]">
       {pane !== "signal" && (
       <Layer name="Chart">
-      <div className="flex min-h-[280px] flex-col gap-3">
-        <div className="panel relative h-[min(58vh,640px)] min-h-[260px] overflow-hidden">
+      <div className="flex min-h-[240px] flex-col gap-3">
+        <div className="panel relative h-[min(46vh,460px)] min-h-[240px] overflow-hidden">
           <CandleChart
             candles={candles}
             forming={forming}
@@ -438,13 +413,17 @@
             levels={outlook?.levels ?? []}
           />
         </div>
-        <PathOutlook />
-        <ScoreBoard />
+        <div className="grid gap-3 lg:grid-cols-2">
+          <ScoreBoard />
+          <UniverseBoard />
+          <AccountBox />
+          <div className="flex flex-col gap-3">
+            <PathOutlook />
+            <WireNotes />
+            <TapeReadout asset={asset} />
+          </div>
+        </div>
         <TradeBook />
-        <WireNotes />
-        <UniverseBoard />
-        <AccountBox />
-        <TapeReadout asset={asset} />
         {density === "advanced" && (
           <div className="grid grid-cols-2 gap-2 sm:grid-cols-4">
             <Mini
@@ -484,7 +463,7 @@
           {rows.length === 0 ? "SCAN…" : hits.length ? `${hits.length} READY` : "FLAT"}
         </p>
       </div>
-      <div className="grid grid-cols-4 gap-1 text-xs">
+      <div className="grid grid-cols-4 gap-1 text-sm">
         <p className="kicker px-1 pb-1">Pair</p>
         {tfs.map((tf) => (
           <p key={tf} className="kicker px-1 pb-1">
@@ -594,6 +573,9 @@
   const dec = useLab((s) => s.scanDec);
   const [scan, setScan] = useState<{
     ms?: number;
+    mood?: string;
+    tape?: string | null;
+    source?: string;
     finviz?: { ticker: string; change: string }[];
     messari?: { symbol: string; rank: number }[];
     finvizError?: string | null;
@@ -620,25 +602,19 @@
     };
   }, []);
 
-  const moodLine =
-    mood === "unknown" || adv == null
+  const moodLine = scan?.tape
+    ? `${scan.tape}. Mood ${scan.mood ?? mood}. Used on bitcoin, gold, and dollar-yen only.`
+    : mood === "unknown" || adv == null
       ? "Stock mood loading"
       : `US stocks ${adv.toFixed(0)}% up, ${dec == null ? "—" : dec.toFixed(0)}% down · ${mood}. Used on bitcoin, gold, and dollar-yen only.`;
 
   return (
-    <div className="border-b border-border bg-surface px-3 py-2 lg:px-4">
-      <div className="mb-1 flex items-center justify-between gap-2">
-        <p className="kicker">Market scan</p>
-        <p className="font-mono text-xs text-muted">{scan?.ms != null ? `${scan.ms} ms` : "loading"}</p>
-      </div>
-      <p className="truncate font-mono text-xs text-fg">{moodLine}</p>
-      <p className="truncate font-mono text-xs text-muted">
-        Finviz{" "}
-        {(scan?.finviz ?? []).slice(0, 6).map((row) => `${row.ticker} ${row.change}`).join("  ·  ") || scan?.finvizError || "…"}
-      </p>
-      <p className="truncate font-mono text-xs text-muted">
-        Messari {(scan?.messari ?? []).map((row) => `${row.rank}.${row.symbol}`).join("  ") || scan?.messariError || "…"}
-        {"  ·  "}Invo login only
+    <div className="border-b border-border bg-bg px-3 py-2 lg:px-4">
+      <p className="truncate text-sm text-muted">
+        <span className="text-fg">Market</span>
+        {" · "}
+        {moodLine}
+        {(scan?.finviz ?? []).length > 0 ? ` · ${scan!.finviz!.slice(0, 4).map((row) => `${row.ticker} ${row.change}`).join(" · ")}` : ""}
       </p>
     </div>
   );
