/**
 * Layer 6 — desk agents.
 * Browser code may import this file. It must not import desk-io (that one touches the disk).
 */
export { postDesk } from "../desk-client";
export type { DeskStatus } from "../desk-logic";
